---
description: 长对话 / 满上下文时保持执行密度，减少敷衍与无意义「新开对话」引导
alwaysApply: true
---

## 长上下文与 Cursor 产品行为（说明）

- **「新开对话 / 新窗口」类提示**：多来自 Cursor 在**上下文变长或接近上限**时的产品策略，用于避免卡顿与丢上下文。
- **「变懒、不想干」体感**：常见于**长线程 + 噪声上下文**下模型的拒答、过度概括、少调用工具；可通过本规则约束**行为**，无法关闭编辑器里的产品提示。

## 在本线程内应怎么做（硬性）

- **除非用户明确说「新开一个 Chat / 换窗口」**，不要主动劝用户放弃当前对话；应在**同一线程**内用**短摘要承接** + **分步执行**把事做完。
- **禁止未行动先推脱**：不说「上下文太长所以我只能粗略…」「建议你重头开对话」等，除非**已经**用工具读过相关文件/跑过命令仍确实缺信息。缺信息时**先 `read_file` / `grep` / 终端**，再结论。
- **执行优先于解释**：长线程里优先**直接改代码、跑命令、查仓库**；同一轮先给出**可验证结果**，说明文字保持紧凑。
- **跨多轮大任务**：可在回复开头用 **3～6 条**列「当前共识 / 未结项 / 下一步」，再接着干，减少在噪声里迷路。
- **与 gjw 规则并存**：长任务仍遵守 `gjw.mdc` / `mcp-messenger.mdc` 的 `check_messages` 工作流。

## 可选（按需由用户打开）

- 若希望**仅**在 Agent 模式生效，可将本文件 front matter 改为 `alwaysApply: false`，并在需要时用 `@long-context-agent` 引用。